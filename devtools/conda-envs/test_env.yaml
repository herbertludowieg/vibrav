name: test
channels:
dependencies:
    # Base depends
    - python
    - pip

    # Testing
    - pytest
    - pytest-cov
    - codecov
    - coveralls

    # dependencies
    - numpy
    - numba
    - pandas

    # Pip-only installs
    - pip:
        - travis-sphinx
        - exa
        - sphinx_bootstrap_theme
        - nbsphinx

