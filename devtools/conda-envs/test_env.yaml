name: test
channels:
dependencies:
    # Base depends
    - python
    - pip

    # Testing
    - pytest
    - pytest-cov
    - codecov

    # dependencies
    - numpy
    - numba
    - sphinx_rtd_theme
    - pandas

    # Pip-only installs
    - pip:
        - travis-sphinx
        - exa

